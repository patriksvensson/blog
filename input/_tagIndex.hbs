<h1>All Tags</h1>

<div id="content">
    <ul class="actions small">
        {{#each tags}}
        {{#if has_posts}}
        <li><a role="button" href="{{link}}" class="button small ">{{title}} ({{count}})</a></li>
        {{/if}}
        {{/each}}
    </ul>
</div>